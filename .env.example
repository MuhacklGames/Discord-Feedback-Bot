## `.env.example`
```env
# Discord bot token (from Developer Portal → Bot)
DISCORD_TOKEN=YOUR_BOT_TOKEN

# Your server (guild) ID
GUILD_ID=YOUR_GUILD_ID

# The forum channel where feedback threads are created
FORUM_CHANNEL_ID=FORUM_CHANNEL_ID

# A regular TEXT channel where the bot opens temporary intake threads
INTAKE_PARENT_CHANNEL_ID=TEXT_CHANNEL_ID

# Optional: deep link to the exact panel message (Copy Message Link in Discord)
PANEL_MESSAGE_URL=

# Optional: fallback jump target if PANEL_MESSAGE_URL isn't set (a channel/thread ID)
PANEL_TARGET_ID=

# Optional: CSV of allowed reaction emojis (names or IDs)
EMOJI_WHITELIST=✨,👍,📌,🚨,❤️,💡,⚠️,⚖️,🧰,🚀,🌍,🧭,🖱️,🎮,📈,💰,♿,👥

# If you want to parse links from intake text messages
USE_MESSAGE_CONTENT=true
